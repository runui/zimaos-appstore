name: stremio
services:
  stremio:
    container_name: stremio
    deploy:
      resources:
        limits:
          memory: 1024M
    environment:
    - NO_CORS=1
    - AUTO_SERVER_URL=1
    devices:
    - /dev/dri:/dev/dri
    image: tsaridas/stremio-docker:v1.2.5
    volumes:
    - type: bind
      source: /DATA/AppData/$AppID/config
      target: /root/.stremio-server
    ports:
    - target: 11470
      published: '11470'
      protocol: tcp
    - target: 8080
      published: '8100'
      protocol: tcp
    restart: unless-stopped
    network_mode: bridge
    privileged: false
x-casaos:
  main: stremio
  icon: https://cdn.jsdelivr.net/gh/runui/zimaos-appstore@gh-pages/apps/stremio/assets/icon.svg
  title: Stremio
  index: /
  port_map: '8100'
  version: v1.2.5
