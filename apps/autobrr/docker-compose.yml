name: autobrr
services:
  autobrr:
    container_name: autobrr
    image: ghcr.io/autobrr/autobrr:v1.69.0
    network_mode: bridge
    restart: unless-stopped
    environment:
      TZ: $TZ
    ports:
    - target: 7474
      published: '7474'
      protocol: tcp
    volumes:
    - type: bind
      source: /DATA/AppData/$AppID/config
      target: /config
x-casaos:
  main: autobrr
  port_map: '7474'
  title: Autobrr
  icon: https://cdn.jsdelivr.net/gh/runui/zimaos-appstore@gh-pages/apps/autobrr/assets/icon.svg
  version: v1.69.0
