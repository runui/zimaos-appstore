name: immich
services:
  immich-server:
    deploy:
      resources:
        reservations:
          memory: 1024M
    container_name: immich-server
    hostname: immich-server
    image: altran1502/immich-server:v2.5.3
    volumes:
    - /DATA/Gallery/immich:/usr/src/app/upload
    - /etc/localtime:/etc/localtime:ro
    environment:
      DB_DATABASE_NAME: immich
      DB_PASSWORD: postgres
      DB_USERNAME: postgres
    ports:
    - 2283:2283
    depends_on:
    - redis
    - database
    restart: unless-stopped
    healthcheck:
      disable: false
    networks:
    - immich
  immich-machine-learning:
    container_name: immich-machine-learning
    hostname: immich-machine-learning
    image: altran1502/immich-machine-learning:v2.5.3
    environment:
      DB_DATABASE_NAME: immich
      DB_PASSWORD: postgres
      DB_USERNAME: postgres
    restart: unless-stopped
    volumes:
    - /DATA/AppData/immich/model-cache:/cache
    healthcheck:
      disable: false
    networks:
    - immich
  redis:
    container_name: immich-redis
    hostname: immich-redis
    image: docker.io/redis:6.2-alpine@sha256:148bb5411c184abd288d9aaed139c98123eeb8824c5d3fce03cf721db58066d8
    healthcheck:
      test: redis-cli ping || exit 1
    volumes:
    - /DATA/AppData/immich/redis:/data
    restart: unless-stopped
    networks:
    - immich
  database:
    container_name: immich-postgres
    hostname: immich-postgres
    image: docker.io/tensorchord/pgvecto-rs:pg14-v0.2.0@sha256:739cdd626151ff1f796dc95a6591b55a714f341c737e27f045019ceabf8e8c52
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: immich
      POSTGRES_INITDB_ARGS: --data-checksums
    volumes:
    - /DATA/AppData/immich/pgdata:/var/lib/postgresql/data
    healthcheck:
      test: pg_isready --dbname='immich' --username='postgres' || exit 1; Chksum="$$(psql
        --dbname='immich' --username='postgres' --tuples-only --no-align --command='SELECT
        COALESCE(SUM(checksum_failures), 0) FROM pg_stat_database')"; echo "checksum
        failure count is $$Chksum"; [ "$$Chksum" = '0' ] || exit 1
      interval: 5m
      start_interval: 30s
      start_period: 5m
    command:
    - postgres
    - -c
    - shared_preload_libraries=vectors.so
    - -c
    - search_path="$$user", public, vectors
    - -c
    - logging_collector=on
    - -c
    - max_wal_size=2GB
    - -c
    - shared_buffers=512MB
    - -c
    - wal_compression=on
    restart: unless-stopped
    networks:
    - immich
networks:
  immich:
    name: immich
x-casaos:
  main: immich-server
  icon: https://cdn.jsdelivr.net/gh/runui/zimaos-appstore@gh-pages/apps/immich/assets/icon.svg
  title: Immich
  port_map: '2283'
  scheme: http
  index: /
  version: v2.7.2
