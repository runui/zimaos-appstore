name: downtify
services:
  downtify:
    environment:
      PGID: $PGID
      PUID: $PUID
      TZ: $TZ
    image: ghcr.io/henriquesebastiao/downtify:1.1.1
    deploy:
      resources:
        reservations:
          memory: 2048M
    ports:
    - target: 8000
      published: '8582'
      protocol: tcp
    restart: unless-stopped
    volumes:
    - type: bind
      source: /DATA/Downloads/downtify
      target: /downloads
    container_name: downtify
x-casaos:
  main: downtify
  icon: https://cdn.jsdelivr.net/gh/runui/zimaos-appstore@gh-pages/apps/downtify/assets/icon.svg
  title: Downtify
  index: /
  port_map: '8582'
  version: 2.6.0
