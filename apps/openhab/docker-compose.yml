name: openhab
services:
  openhab:
    environment:
      CRYPTO_POLICY: unlimited
      PGID: $PGID
      PUID: $PUID
    image: openhab/openhab:4.3.3
    deploy:
      resources:
        reservations:
          memory: 64M
    network_mode: host
    restart: unless-stopped
    volumes:
    - type: bind
      source: /etc/localtime
      target: /etc/localtime
      read_only: true
    - type: bind
      source: /etc/timezone
      target: /etc/timezone
      read_only: true
    - type: bind
      source: /DATA/AppData/$AppID/addons
      target: /openhab/addons
    - type: bind
      source: /DATA/AppData/$AppID/conf
      target: /openhab/conf
    - type: bind
      source: /DATA/AppData/$AppID/userdata
      target: /openhab/userdata
    container_name: openhab
x-casaos:
  main: openhab
  icon: https://cdn.jsdelivr.net/gh/runui/zimaos-appstore@gh-pages/apps/openhab/assets/icon.svg
  title: OpenHAB
  index: /
  port_map: '8080'
  version: 4.3.3
